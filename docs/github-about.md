# About do repositório (GitHub)

Use o conteúdo abaixo na página do repositório: **Settings** (ou o ícone de engrenagem) → secção **About** → **Edit repository details**.

---

## Descrição curta (campo *Description*)

Copie **uma** das linhas (máx. ~350 caracteres no GitHub):

**Português**

> Chat estilo assistente com React e Node: respostas em streaming via SSE, API OpenAI no servidor e UI com Markdown.

**English** *(opcional, para alcance)*

> ChatGPT-like UI with React + Fastify: OpenAI streaming over SSE, secure API key on the server, Markdown replies.

---

## Website

- Deixe em branco se não houver deploy público, ou coloque a URL do frontend em produção (ex.: `https://chat.exemplo.com`).

---

## Topics (etiquetas)

Sugestão para colar no campo **Topics** (adicionar uma a uma ou separadas por Enter no GitHub):

```
nodejs
typescript
react
vite
fastify
openai
sse
server-sent-events
streaming
chatgpt
chat-ui
monorepo
```

Outras opcionais: `zod`, `tailwindcss`, `markdown`, `api-proxy`, `portuguese`.

---

## Resumo para a wiki ou *Social preview*

**Studio Chat** — monorepo com frontend React (Vite, Tailwind) e backend Fastify. O utilizador escolhe o modelo OpenAI permitido pelo servidor; as respostas chegam em tempo real por SSE. A chave `OPENAI_API_KEY` existe só no backend.

Repositório: https://github.com/Waelson/studio-chat
